import { PickupLocationPipe } from './pickup-location.pipe';

describe('PickupLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new PickupLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
